<%- include ('../partials/header') %>

<h4>Patient Dashboard</h4>

<% if(currentUserStatus!=null){ %>
    <% var token = (currentUserStatus.token < 10)?'00'+currentUserStatus.token:((currentUserStatus.token < 100)?'0'+currentUserStatus.token:currentUserStatus.token) %>
    <p>Name : <%= currentUserStatus.name %></p>
    <p>Token No : <%= "T" + token %></p>
    <p>Registration Time : <%= currentUserStatus.registrationTime %></p>
    <p>Consultation Queue In Time : <%= currentUserStatus.consultationInTime %></p>
    <p>Consultation Queue Out Time : <%= currentUserStatus.consultationOutTime %></p>
    <p>Billing Queue Out Time : <%= currentUserStatus.billingInTime %></p>
    <p>Billing Queue Out Time : <%= currentUserStatus.billingOutTime %></p>
    <p>Medicine Queue Out Time : <%= currentUserStatus.medicineInTime %></p>
    <p>Medicine Queue Out Time : <%= currentUserStatus.medicineOutTime %></p>
<% }else{ %>
    <p>Patient in not in hospital now</p>
<% } %>

<%- include ('../partials/footer') %>
